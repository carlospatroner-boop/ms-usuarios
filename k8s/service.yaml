apiVersion: v1
kind: Service
metadata:
  name: ms-usuarios-service
  namespace: default
spec:
  selector:
    app: ms-usuarios
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8092
  type: LoadBalancer # IP pública en Azure