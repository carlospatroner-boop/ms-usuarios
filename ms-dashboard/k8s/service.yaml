apiVersion: v1
kind: Service
metadata:
  name: ms-dashboard-service
  namespace: default
spec:
  selector:
    app: ms-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer # Esto te dará OTRA IP pública para el dashboard